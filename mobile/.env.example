# Copy to .env.local for a dev build or the web preview. Production builds read
# `extra.apiBase` from app.json and need none of this.

# Point the app at a local Worker (`npm run dev` in backend/) or at the CORS
# proxy used by `npm run web` (see README → Web preview).
# EXPO_PUBLIC_API_BASE=http://localhost:8787
